models:
  Person:
    nombres: string:255
    apellidos: string:255
    ci: string:9:unique
    correo: string:255
    telefono: integer
    direccion: string:255
    fecha_nacimiento: date
    rol: enum:estudiante,profesor,administrador,contable,tutor

  User:
    person_id: id:fk
    nombre_de_usuario: string:255:unique
    contraseña: string:255
    timestamps: true

controllers:
  PersonController:
    resource: true

  UserController:
    resource: true

routes:
  web:
    resource: persons
